import { TodayDashboard } from "@/features/today/components/today-dashboard"
import { TODAY_DASHBOARD_PREVIEW } from "@/features/today/today-preview"

export default function TodayPage() {
  return <TodayDashboard data={TODAY_DASHBOARD_PREVIEW} />
}
